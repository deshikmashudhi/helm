apiVersion: v1
kind: Service
metadata: 
  name: nginx-service
spec:
  selector:
    app: nginx
    project: roboshop
    component: frontend
  ports: 
  - name: name-of-service-port
    protocol: TCP
    port: 80
    targetPort: http-web-svc